pipeline {
    agent any
    stages {
        stage('Check Git Installation') {
            steps {
                sh 'git --version' // Use bat 'git --version' for Windows
            }
        }
    }
}
